Code is compiled with

python3 dt.py training_set.csv validation_set.csv test_set.csv validation.set.csv yes 1

First three inputs are for the sets to use for training validation and test

4th input is yes or no depends on if you want to print the tree

5th input is your choice of heuristic 1 for gain 2 for VI